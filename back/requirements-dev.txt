# Requirements Dev.
#
# This file supports the FastAPI backend for GenAI Inventory.
#
# The file provides:
# - backend helper functions or scripts.
# - shared runtime behavior for API and maintenance workflows.
#
# Key Structures Used:
# - Python modules, environment settings, and database helpers.
#
# This file ensures:
# - backend workflows remain organized by responsibility.
# - scripts can be run for local debugging and maintenance.
#
# Editors: Aniket, Dipanker, Liam, Jin, and Philip.
pytest>=8.0
pytest-mock>=3.14          # pytest.mock fixture (wrapper around unittest.mock)
pytest-asyncio>=0.23       # async test support
pytest-cov>=5.0            # coverage reports

# HTTP testing for FastAPI (TestClient uses httpx under the hood with starlette)
httpx>=0.27

# Lightweight in-memory SQLite (used for DB integration tests)
# psycopg2 already in requirements.txt; aiosqlite for async tests
aiosqlite>=0.20

# No separate bcrypt stub package is required for this repo.
