[phases.setup]
nixPkgs = ['python312', 'postgresql_16.dev', 'gcc']

[phases.install]
cmds = [
  'python -m venv --copies /opt/venv && . /opt/venv/bin/activate && pip install -r back/requirements.txt'
]

[start]
cmd = '/opt/venv/bin/python -m uvicorn back.main:app --host 0.0.0.0 --port $PORT'
